# agrega reglas (si ya existe, se añaden al final)
@"
# secrets
.env
*.env
.env.*
*.pem
*.key
"@ | Out-File -FilePath .gitignore -Encoding utf8 -Append

git rm --cached .env
git add .gitignore
git commit -m "chore: add .gitignore and stop tracking .env"
